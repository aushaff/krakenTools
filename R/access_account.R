access_account <- function(data_dir) {
  # data_dir <- "/home/deckard/Desktop/kraken_data"
  # list.files(data_dir)
  # key <- read.table(file.path(data_dir, "kraken_api.txt"))
  #
  # api_key <- strsplit(as.character(key[1,]),":")[[1]][2]
  # priv_key <- strsplit(as.character(key[2,]),":")[[1]][2]
  #
  # base_url <- "https://api.kraken.com/0/private/TradeBalance"
  #
  # install.packages("Rbitcoin")
  # library(Rbitcoin)
  #
  # market.api.query.kraken(base_url, api_key, priv_key)
  #
  # market.api.process(market = "kraken", currency_pair = c("BTC", "EUR"), action = "wallet",
  #                    key = api_key, secret = priv_key)
  # market.api.process

}
